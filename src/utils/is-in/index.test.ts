import { initialGrid, isInColumn, isInRow } from 'utils'

describe('isInCol', () => {
    it('Returns true when value is in Grid column', () => {

    })

    it('Returns false when value is not in Grid column', () => {

    }) 
})